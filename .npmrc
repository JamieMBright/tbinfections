# NPM Configuration for TB Infections Simulator
# This file ensures consistent dependency resolution across environments

# Use legacy peer dependency resolution
# Required because react-simple-maps@3.0.0 hasn't updated peer deps for React 19
legacy-peer-deps=true

# Ensure package-lock.json is always updated
package-lock=true

# Use exact versions when installing
save-exact=false

# Engine strictness (optional, uncomment to enforce Node version)
# engine-strict=true
